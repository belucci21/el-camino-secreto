export function ExperienceLoader() {
  return (
    <section className="scene loader" aria-labelledby="loader-title">
      <span className="sigil" aria-hidden="true">
        ✦
      </span>
      <h2 id="loader-title">El camino está despertando.</h2>
    </section>
  );
}
